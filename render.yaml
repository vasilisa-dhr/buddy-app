services:
  - type: web
    name: buddy-app
    env: node
    buildCommand: npm install
    startCommand: npm start
    plan: free
    healthCheckPath: /admin/health
    envVars:
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_ANON_KEY
        sync: false


